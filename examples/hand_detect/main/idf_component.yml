## IDF Component Manager Manifest File
dependencies:
  espressif/hand_detect:
    version: "*"
    override_path: "../../../models/hand_detect"
  espressif/esp32_p4_function_ev_board_noglib:
    version: "*"
    rules:
     - if: "target == esp32p4"
  espressif/esp32_s3_eye_noglib:
    version: "*"
    rules:
     - if: "target == esp32s3"
